GOOGLE_CLOUD_PROJECT=your-gcp-project-id
CHAT_SPACE_NAME=spaces/XXXXXXX
PORT=8080
# Local dev only: point to a service account key file for Vertex AI auth
# In Cloud Run, ADC uses the attached service account automatically
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json
